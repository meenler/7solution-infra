apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "7solution-infra.fullname" . }}-test-connection"
  labels:
    {{- include "7solution-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "7solution-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
